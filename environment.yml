name: quantum-gates
channels:
  - defaults
dependencies:
  - python=3.9
  - setuptools
  - pip:
    - numpy
    - qiskit==2.0
    - qiskit-aer==0.17.0
    - qiskit-ibm-runtime==0.39.0
    - scipy
    - matplotlib
    - pylatexenc
    - opt-einsum
    - pytest
    - python-dotenv
    - build
